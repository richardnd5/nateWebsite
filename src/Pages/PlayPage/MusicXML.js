

const createStuff = (word) => {
    let newWord = `${word} what of it?`
    return newWord
}

export const returnString = () => { 
    let wordToReturn = createStuff('here it is!')
    return wordToReturn
}

